
const state = {
  ws: null
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {
  /**
   * 设置表单对象信息
   */
  setWS(state, ws) {
    state.ws = ws;
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
